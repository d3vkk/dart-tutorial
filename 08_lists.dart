void main() {
    // In Dart, arrays are called lists
    var primeNumbers = [2, 3, 5, 7];

    //add one item to list
    primeNumbers.add(11);

    //add multiple items to list
    primeNumbers.addAll([13, 17, 19, 23]);

    print(primeNumbers);

    //Additional inbuilt list manipulation methods are available
}
